import React from "react";
import NavMenu from "./NavMenu";

const NavBar = () => {
    return (
        <div>
            <h1>Catch the Bus SG</h1>
            <NavMenu />
        </div>
    );
};

export default NavBar;
